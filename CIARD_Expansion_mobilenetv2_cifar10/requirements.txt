# Minimal runtime dependencies. The project is verified in the existing
# `ciard` environment (Python 3.8, torch 1.10.0+cu113,
# torchvision 0.11.1+cu113). safe_torch_load keeps newer torch compatible.
torch>=1.10
torchvision>=0.11
numpy>=1.22
loguru>=0.7
torchattacks>=3.5
# AutoAttack is imported as `autoattack`; use the installation already present
# in the project environment or the official fra31/auto-attack package.
