# Core dependencies for SARD (CIARD IJCV extension)
# Install PyTorch separately matching your CUDA version:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# The original requirements.txt pinned torch==1.10.0 which is incompatible
# with weights_only=False (requires PyTorch >=2.6). We now require >=2.0.
# The code uses safe_torch_load() for backward compatibility.

torch>=2.0.0
torchvision>=0.15.0
loguru>=0.7.0
torchattacks>=3.5.0
autoattack>=0.1.0
robustbench>=0.1.0
numpy>=1.22.0
pillow>=10.0.0
